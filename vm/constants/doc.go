// Package constants defines VM-wide numeric limits and the canonical error
// values that contract execution returns.
//
// # Overview
//
// constants centralizes plasma costs, block-size limits, gas-equivalent caps
// for embedded-contract calls, and the named errors that the verifier and
// embedded-contract implementations return so callers can branch on them.
//
// Per-package documentation is being filled in incrementally. See
// docs/STYLE.md for the full template applied in subsequent PRs.
package constants
