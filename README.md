[![Ask DeepWiki](https://deepwiki.com/badge.svg)](https://deepwiki.com/zenon-network/go-zenon)

# Zenon Node

Reference Golang implementation of the Alphanet - Network of Momentum Phase 0.

## Building from source

Building `znnd` requires both a Go (version 1.16 or later) and a C compiler. You can install them using your favourite package manager. Once the dependencies are installed, please run:

```shell
make znnd
```

## Running `znnd`

Since version `0.0.2`, `znnd` is configured with the Alphanet Genesis and default seeders.

Use [znn-controller](https://github.com/zenon-network/znn_controller_dart) to configure your full node. For more information please consult the [Wiki](https://github.com/zenon-network/znn-wiki).

## Documentation

- **API reference (godoc)**: [`docs/api/`](docs/api/README.md) — every package's godoc rendered as markdown, regenerated by `make doc-api`. After upstream merge this also publishes to [pkg.go.dev](https://pkg.go.dev/github.com/zenon-network/go-zenon).
- **Architecture**: [`ARCHITECTURE.md`](ARCHITECTURE.md) — system overview, dual-ledger model, concept glossary.
- **Agent / contributor map**: [`AGENTS.md`](AGENTS.md) — package-by-package one-liners and where-to-look index.
- **Godoc style guide**: [`docs/STYLE.md`](docs/STYLE.md) — conventions for writing package and exported-symbol comments.

For local interactive browsing: `make docs` (serves godoc on `:6060`).
